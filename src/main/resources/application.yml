server:
  port: 8081

spring:
  application:
    name: pdfprocessor-api-backend
  data:
    mongodb:
      # URI com parâmetros de timeout e retry para melhor resiliência
      # Timeouts aumentados e configurações otimizadas para reduzir warnings durante manutenção do pool
      uri: mongodb+srv://concursosuser:YUWjEG1aR8ZZVAkp@fenaeprodcluster.f4rqt.mongodb.net/pdfprocessor?retryWrites=true&w=majority&appName=FenaeProdCluster&serverSelectionTimeoutMS=60000&connectTimeoutMS=60000&socketTimeoutMS=60000&maxPoolSize=50&minPoolSize=5&maxIdleTimeMS=600000&heartbeatFrequencyMS=15000&serverSelectionRetryDelayMS=5000&retryReads=true&retryWrites=true
      # uri: mongodb+srv://rbrodrigues_db_user:PghGGyWyVMCY9945@prod.ewbwotp.mongodb.net/pdfprocessor?appName=Prod&retryWrites=true&w=majority&serverSelectionTimeoutMS=60000&connectTimeoutMS=60000&socketTimeoutMS=60000&maxPoolSize=50&minPoolSize=5&maxIdleTimeMS=600000&heartbeatFrequencyMS=15000&serverSelectionRetryDelayMS=5000&retryReads=true&retryWrites=true

jwt:
  secret: my-super-secret-key-that-must-be-very-long-and-secure-at-least-256-bits
  expiration: 43200000 # 12 horas
  refresh-expiration: 2592000000 # 30 dias (conforme documentação)

app:
  security:
    force2fa-global: false # Forçar 2FA globalmente (pode ser ativado por tenant ou usuário)
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
  logging:
    mongo:
      enabled: false

text-extraction:
  tesseract:
    # Path to tessdata directory
    datapath: "D:/dev/tessdata-main/tessdata-main"
    language: "por"  # Portuguese
    dpi: 300  # DPI for PDF to image conversion

