package br.com.verticelabs.pdfprocessor.infrastructure.config;

/**
 * Seed de rubricas removido — rubricas são cadastradas exclusivamente via API
 * (POST /api/v1/rubricas).
 *
 * Classe mantida vazia para evitar erros de compilação em caso de referências,
 * mas não contém nenhuma lógica de inicialização.
 */
public class RubricaDataInitializer {
    // intencionalmente vazio
}
