# ============================================
# PDF Processor API - Environment Variables
# ============================================
# Copie este arquivo para .env e configure os valores

# MongoDB Atlas URI (substitua pela sua)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/pdfprocessor?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-key-minimum-256-bits-for-hs256
JWT_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=2592000000

# MongoDB Local (apenas se usar profile "with-mongodb")
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=admin123
