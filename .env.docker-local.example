# ============================================
# PDF Processor API - Teste local com Docker
# ============================================
# Copie este arquivo para .env (ou deixe o INICIAR.bat fazer isso)
# Use este arquivo quando for subir API + MongoDB com os scripts INICIAR.bat / PARAR.bat

# MongoDB dentro do Docker (não altere a menos que mude usuário/senha no docker-compose)
MONGODB_URI=mongodb://admin:admin123@mongodb:27017/pdfprocessor?authSource=admin

# JWT (pode deixar assim para teste)
JWT_SECRET=chave-secreta-teste-minimo-256-bits-para-hs256
JWT_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=2592000000

# Credenciais do MongoDB local (usadas pelo container MongoDB)
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=admin123
